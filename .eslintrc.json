{
    "extends": "airbnb-base",
    "plugins": [
        "import"
    ],
    "rules": {
      "strict": 0,
      "no-param-reassign": ["error", { "props": false }],
      "no-console": ["error", { "allow": ["warn", "error", "log"] }]
    }
}
